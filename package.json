{
  "name": "pixelcampus",
  "version": "1.0.0",
  "description": "Website and API for the PixelCampus Minecraft server.",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "test": "npm --prefix shared run test && npm --prefix backend run test && npm --prefix frontend test",
    "lint": "npm --prefix backend run lint && npm --prefix frontend run lint"
  }
}
